// Array Declaration
    let array = [1,2,3,4,5];
    console.log(array);
    // console.log(array.length);

// while loop
    // let i = 0;;
    // while(i < array.length){
    //     console.log("element at index", i," is ", array[i]);
    //     i++;
    // }

// Array Functions : push -- last ma value add krva
// Array Functions : unshift -- agal value add krva
// Array Functions : shift -- Start value remove krva
// Array Functions : pop -- last value remove krva

    // array.push("Last value add");
    // array.unshift("First Value remove");
    // console.log(array);
    // array.shift("Last value remove");    
    // array.pop("First Value add");
    // console.log(array);

//  slice function --- for fetch some particular data from index no. to index no.
    // let partOfAnArray = array.slice(2,4); 
    // console.log(partOfAnArray);
    // console.log(array);

//  splice function --- Delete array data from index no. to how much index - kya index thi ketla delete krvana e
    array.splice(2,1); // index 2 thi 1 sudhi delete krse ( 1 2 3 4 5 ), 3 delete krse
    array.splice(2,3);
    console.log(array);

// indexOf function --- array index print element kya chhe e
// contain --- element chhe ke nhi e check krva







     